# Review package: 3804e30..19f077a

## Commits
19f077a feat: add report page with monthly summary and CSV export

## Files changed
 miniapp/miniprogram/pages/report/index.js   |  58 ++++++++++++
 miniapp/miniprogram/pages/report/index.json |   3 +
 miniapp/miniprogram/pages/report/index.wxml |  30 ++++++
 miniapp/miniprogram/pages/report/index.wxss | 137 ++++++++++++++++++++++++++++
 4 files changed, 228 insertions(+)

## Diff
diff --git a/miniapp/miniprogram/pages/report/index.js b/miniapp/miniprogram/pages/report/index.js
new file mode 100644
index 0000000..5bf6cca
--- /dev/null
+++ b/miniapp/miniprogram/pages/report/index.js
@@ -0,0 +1,58 @@
+// miniapp/miniprogram/pages/report/index.js
+const { invoices } = require('../../utils/supabase');
+const { CATEGORIES, getLabel } = require('../../utils/categories');
+
+Page({
+  data: {
+    currentMonth: '', summary: null, categoryData: []
+  },
+  onShow() {
+    const now = new Date();
+    this.setData({ currentMonth: `${now.getFullYear()}-${String(now.getMonth()+1).padStart(2,'0')}` });
+    this.loadSummary();
+  },
+  async loadSummary() {
+    try {
+      const all = await invoices.list({ month: this.data.currentMonth });
+      const summary = {
+        total: all.reduce((s, i) => s + parseFloat(i.total_amount || 0), 0),
+        iva: all.reduce((s, i) => s + parseFloat(i.iva_amount || 0), 0),
+        base: all.reduce((s, i) => s + parseFloat(i.base_amount || 0), 0),
+        count: all.length
+      };
+      // 按分类汇总
+      const catMap = {};
+      all.forEach(i => {
+        catMap[i.category] = (catMap[i.category] || 0) + parseFloat(i.total_amount || 0);
+      });
+      const categoryData = CATEGORIES
+        .filter(c => catMap[c.value])
+        .map(c => ({
+          label: c.label,
+          amount: catMap[c.value],
+          pct: summary.total ? Math.round(catMap[c.value] / summary.total * 100) : 0
+        }));
+      this.setData({ summary, categoryData });
+    } catch (e) { wx.showToast({ title: '加载失败', icon: 'none' }); }
+  },
+  async onExport() {
+    wx.showLoading({ title: '生成中...' });
+    try {
+      const all = await invoices.list({ month: this.data.currentMonth });
+      // 生成 CSV（Excel 可直接打开）
+      let csv = 'Fecha,Proveedor,Nº Factura,Base,IVA%,IVA,Total,Categoría\n';
+      all.forEach(i => {
+        csv += `${i.invoice_date},${i.supplier_name},${i.invoice_number || ''},${i.base_amount},${i.iva_rate},${i.iva_amount},${i.total_amount},${getLabel(i.category)}\n`;
+      });
+      // 写入临时文件并分享
+      const fs = wx.getFileSystemManager();
+      const filePath = `${wx.env.USER_DATA_PATH}/invoices_${this.data.currentMonth}.csv`;
+      fs.writeFileSync(filePath, csv, 'utf8');
+      wx.shareFileMessage({
+        filePath, fileName: `invoices_${this.data.currentMonth}.csv`,
+        success() { wx.hideLoading(); wx.showToast({ title: '已分享', icon: 'success' }); },
+        fail() { wx.hideLoading(); wx.showToast({ title: '导出失败', icon: 'none' }); }
+      });
+    } catch (e) { wx.hideLoading(); wx.showToast({ title: '导出失败', icon: 'none' }); }
+  }
+});
diff --git a/miniapp/miniprogram/pages/report/index.json b/miniapp/miniprogram/pages/report/index.json
new file mode 100644
index 0000000..946eb49
--- /dev/null
+++ b/miniapp/miniprogram/pages/report/index.json
@@ -0,0 +1,3 @@
+{
+  "navigationBarTitleText": "月度报表"
+}
diff --git a/miniapp/miniprogram/pages/report/index.wxml b/miniapp/miniprogram/pages/report/index.wxml
new file mode 100644
index 0000000..76a640b
--- /dev/null
+++ b/miniapp/miniprogram/pages/report/index.wxml
@@ -0,0 +1,30 @@
+<!-- miniapp/miniprogram/pages/report/index.wxml -->
+<view class="report-page">
+  <view class="month-header">{{currentMonth}}</view>
+
+  <!-- 汇总卡片 -->
+  <view class="summary-card card" wx:if="{{summary}}">
+    <view class="summary-main">
+      <text class="big-amount">€{{summary.total}}</text>
+      <text class="big-label">本月总金额</text>
+    </view>
+    <view class="summary-details">
+      <view class="detail-item"><text class="detail-val">€{{summary.base}}</text><text class="detail-lbl">Base</text></view>
+      <view class="detail-item"><text class="detail-val">€{{summary.iva}}</text><text class="detail-lbl">IVA</text></view>
+      <view class="detail-item"><text class="detail-val">{{summary.count}}</text><text class="detail-lbl">发票</text></view>
+    </view>
+  </view>
+
+  <!-- 分类占比 -->
+  <view class="category-bars" wx:if="{{categoryData.length > 0}}">
+    <view class="cat-row" wx:for="{{categoryData}}" wx:key="label">
+      <text class="cat-label">{{item.label}}</text>
+      <view class="cat-bar-track">
+        <view class="cat-bar-fill" style="width: {{item.pct}}%"></view>
+      </view>
+      <text class="cat-pct">{{item.pct}}%</text>
+    </view>
+  </view>
+
+  <button class="btn-export" bindtap="onExport">📤 导出 Excel</button>
+</view>
diff --git a/miniapp/miniprogram/pages/report/index.wxss b/miniapp/miniprogram/pages/report/index.wxss
new file mode 100644
index 0000000..3a952ae
--- /dev/null
+++ b/miniapp/miniprogram/pages/report/index.wxss
@@ -0,0 +1,137 @@
+/* miniapp/miniprogram/pages/report/index.wxss */
+.report-page {
+  padding: 16px;
+  padding-bottom: 40px;
+  min-height: 100vh;
+}
+
+.month-header {
+  font-size: 18px;
+  font-weight: 600;
+  text-align: center;
+  color: var(--ink-blue);
+  padding: 12px 0 16px;
+}
+
+/* 汇总卡片 */
+.summary-card {
+  margin-bottom: 24px;
+}
+
+.summary-main {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  padding: 16px 0 12px;
+}
+
+.big-amount {
+  font-size: 36px;
+  font-weight: bold;
+  color: var(--ink-blue);
+  line-height: 1.2;
+}
+
+.big-label {
+  font-size: 14px;
+  color: var(--steel-gray);
+  margin-top: 4px;
+}
+
+.summary-details {
+  display: flex;
+  justify-content: space-around;
+  padding-top: 16px;
+  border-top: 1px solid var(--pale-ink);
+  margin-top: 8px;
+}
+
+.detail-item {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  min-width: 64px;
+}
+
+.detail-val {
+  font-size: 20px;
+  font-weight: 600;
+  color: var(--ink-black);
+}
+
+.detail-lbl {
+  font-size: 12px;
+  color: var(--steel-gray);
+  margin-top: 2px;
+}
+
+/* 分类占比 */
+.category-bars {
+  background: var(--paper-white);
+  border-radius: 12px;
+  padding: 20px 16px;
+  margin-bottom: 24px;
+  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
+}
+
+.cat-row {
+  display: flex;
+  align-items: center;
+  margin-bottom: 14px;
+}
+
+.cat-row:last-child {
+  margin-bottom: 0;
+}
+
+.cat-label {
+  width: 80px;
+  font-size: 14px;
+  color: var(--ink-black);
+  flex-shrink: 0;
+}
+
+.cat-bar-track {
+  flex: 1;
+  height: 12px;
+  background: var(--paper-texture);
+  border-radius: 6px;
+  margin: 0 10px;
+  overflow: hidden;
+}
+
+.cat-bar-fill {
+  height: 100%;
+  background: var(--ink-blue);
+  border-radius: 6px;
+  transition: width 0.3s ease;
+}
+
+.cat-pct {
+  width: 36px;
+  font-size: 13px;
+  color: var(--steel-gray);
+  text-align: right;
+  flex-shrink: 0;
+}
+
+/* 导出按钮 */
+.btn-export {
+  width: 100%;
+  height: 48px;
+  line-height: 48px;
+  background-color: var(--ink-blue);
+  color: #fff;
+  border-radius: 10px;
+  font-size: 17px;
+  font-weight: 500;
+  border: none;
+}
+
+.btn-export::after {
+  border: none;
+}
+
+.btn-export:active {
+  opacity: 0.8;
+}
